# Perseus 1 Mission Pack — Python dependencies
# Install with: pip install -r requirements.txt

# Mission control server
flask>=3.0
flask-socketio>=5.3
simple-websocket>=1.0     # WebSocket transport for threading mode

# Telemachus WebSocket client
websocket-client>=1.7

# Simulation utilities (all stdlib except below)
# No additional dependencies for sim/ — pure Python 3.12+

# Optional: for running diagram generation
# (only needed if regenerating SVG sheets)
# No extra deps — diagrams use stdlib only
